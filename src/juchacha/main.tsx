import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import { JuchachaApp } from './JuchachaApp';

ReactDOM.createRoot(document.getElementById('juchacha-root')!).render(
  <React.StrictMode>
    <JuchachaApp />
  </React.StrictMode>,
);




