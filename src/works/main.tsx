import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import { WorksApp } from './WorksApp';

ReactDOM.createRoot(document.getElementById('works-root')!).render(
  <React.StrictMode>
    <WorksApp />
  </React.StrictMode>,
);




